# TripSync — Backend

Reserved for the TripSync backend (to be built in a later milestone).

Planned scope: trip persistence, document storage, notification/push hooks, and (optionally) a live disruption feed. Nothing is implemented yet — the current prototype runs fully client-side from the `frontend/` app.